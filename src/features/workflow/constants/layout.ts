// =============================================================================
// Node Dimensions
// =============================================================================

/** Width of trigger and action nodes in pixels (includes padding) */
export const NODE_WIDTH = 320;

/** Height of trigger and action nodes in pixels (actual rendered height) */
export const NODE_HEIGHT = 98;

/** Internal padding within nodes in pixels */
export const NODE_PADDING = 12;

/** Size of plus helper nodes (width and height) in pixels */
export const PLUS_NODE_SIZE = 32;

// =============================================================================
// Spacing
// =============================================================================

/** Vertical spacing between consecutive nodes in pixels */
export const VERTICAL_SPACING = 48;

/** Horizontal spacing between branch lanes for if-else nodes */
export const BRANCH_HORIZONTAL_SPACING = 320;

// =============================================================================
// Starting Positions
// =============================================================================

/** X coordinate for center of main flow */
export const CANVAS_CENTER_X = 400;

/** Y coordinate for first node */
export const CANVAS_START_Y = 100;
